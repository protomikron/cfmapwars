# Overview

A simple map simulation game with territories and stuff.

# Run

You need Python with SDL2 and GDAL libraries.

    $ python3 -m venv .venv
    $ source .venv/bin/activate
    (.venv) $ pip install -r requirements.txt
    (.venv) $ python3 
    (.venv) $ python3 main.py res/map_shp/

That should do the work.

# FAQ

Q: Why?
A: Why not?

# License

MIT
